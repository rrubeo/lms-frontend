const API_PAESE = `${process.env.server}/cbpaese`;
const API_REGIONE = `${process.env.server}/cbregione`;
const API_PROVINCIA = `${process.env.server}/cbprovincia`;
const API_COMUNE = `${process.env.server}/cbcomune`;

const API_CLASSE = `${process.env.server}/gstu/cbclasse`;
const API_ARGOMENTO = `${process.env.server}/gstu/cbargomento`;
const API_LEZIONE = `${process.env.server}/gstu/cblezione`;

const API_DOCENTE = `${process.env.server}/gstu/cbdocente`;

module.exports = {
  API_PAESE,
  API_REGIONE,
  API_PROVINCIA,
  API_COMUNE,
  API_CLASSE,
  API_ARGOMENTO,
  API_LEZIONE,
  API_DOCENTE,
};
